Program for computing the max spacing in k-clustering 
(application of Kruskal's minimum spanning tree algorithm)

The task was to program a clustering algorithm where the target number of clusters is 4.
To compute the max spacing of a 4-clustering for a given graph.
